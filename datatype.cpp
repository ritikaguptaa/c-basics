//DATATYPES: if you are taking a number n, you have to store somewhere in your program,the place where you store it can be a data gtype.
//interger data tyes(int,long , long long )
//float,double(to store after decimal points)
//string and getline(to store string )
//char (to stored characters)

#include<bits/stdc++.h>//it includes all the libraries (it takes a bit more time but its fine it is negligible as compared to time taken by your program to run, instead of writing so many #include)
using namespace std;

int main(){
    //integer
    // int x=10;
    // cout<<x;
    //return 0;

    //long int 
    // long int y=123456;
    // cout<<y;
    // return 0;

    //long long int
    // long long int z=125474893;
    // cout<<z;
    // return 0;

    //float,double
    // float f=12.5657;//32bits size
    // float f=12; // an integer can be stored in float or double 
//     double f=1223.849087;//64 bits size
//     cout<<f;
//     return 0;
// }

//STRING:(it will picks up the word before space)
// string s;
// cin>>s;
// cout<<s;
// return 0;

// string s1,s2;
// cin>>s1>>s2;
// cout<<s1<<" "<<s2;
// return 0;

 //all string together:
//  string str;
//  getline(cin,str);//it picks up till the line break 
//  cout<<str;
//  return 0;

//CHAR:to store character (' ')//  having 256 character in english dictionary it stores all .
//  char ch='h';
char ch;
cin>>ch;
 cout<<ch;
 return 0;
}
//WHICH DATA TYPE WE SHOUD USE TO STORE 10?
//WE CAN STORE IT IN LONG INT BUT IT IS NOT A GOODNIDEA BCZ IT TAKES MORE SPACE EVERYTHING WILL TAKE MEMORY IN YOUR COMPUTER 